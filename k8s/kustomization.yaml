apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flowforge

labels:
  - pairs:
      app.kubernetes.io/part-of: flowforge-platform

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.example.yaml
  - postgres.yaml
  - rabbitmq.yaml
  - redis.yaml
  - backend.yaml
  - hpa.yaml
  - ingress.yaml
